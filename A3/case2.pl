node(a).

node(b).

node(c).

node(d).

node(e).

node(f).

node(g).

edge(a,b).

edge(b,c).

edge(a,c).

edge(c,d).

edge(a,d).

edge(b,e).

edge(e,d).

edge(e,f).

edge(d,f).

edge(f,g).

edge(e,g).

edge(d,g).
